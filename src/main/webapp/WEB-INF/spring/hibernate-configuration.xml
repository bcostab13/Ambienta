<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:util="http://www.springframework.org/schema/util" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">
    
    <!-- Archivo de propiedades para la conección a base de datos -->	
    <context:property-placeholder
    location="classpath:spring/jdbc/produccion.properties" />
    
    <bean id="dataSourceAmbienta" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.config.driver}" />
        <property name="url" value="${jdbc.config.url}" />
        <property name="username" value="${jdbc.config.username}" />
        <property name="password" value="${jdbc.config.password}" />
    </bean>
    
    <util:properties id="jpaProperties">
        <prop key="hibernate.generate_statistics">${hibernate.estadisticas}</prop>
        <prop key="hibernate.format_sql">${hibernate.sql.format}</prop>
    </util:properties>

    <bean id="jpaAdapter"
        class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"
        p:databasePlatform="${hibernate.dialect}" p:showSql="${hibernate.sql.show}" />
    
    <bean id="entityManagerFactory"
        class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean"
        p:dataSource-ref="dataSourceAmbienta" p:jpaVendorAdapter-ref="jpaAdapter" p:jpaProperties-ref="jpaProperties">
        <property name="loadTimeWeaver">
                <bean
                        class="org.springframework.instrument.classloading.InstrumentationLoadTimeWeaver" />
        </property>
        <property name="persistenceUnitName" value="ambientaPU" />
    </bean>	
</beans>